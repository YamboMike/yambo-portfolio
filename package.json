{
  "name": "yambo-portfolio",
  "version": "1.0.0",
  "description": "Mike Yambo — Digital Forensic Investigator portfolio",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "esbuild src/index.jsx --bundle --servedir=public --outfile=public/bundle.js --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
